allOf:
  - $ref: "./ValidationError.yaml"
  - type: object
    properties:
      paths:
        type: array
        description: The missing json paths.
        items:
          type: string
          format: json-path
          description: The json path.
    required:
      - paths
