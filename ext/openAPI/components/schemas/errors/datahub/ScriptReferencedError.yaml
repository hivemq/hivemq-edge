x-implements:
  - com.hivemq.api.errors.OpenApiError
allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      id:
        type: string
        description: The script ID.
      message:
        type: string
        description: The error message.
    required:
      - id
