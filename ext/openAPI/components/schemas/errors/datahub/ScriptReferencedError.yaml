allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      id:
        type: string
        description: The script ID.
      message:
        type: string
        description: The error message.
      status:
        type: integer
        default: 400
        description: The HTTP status code.
    required:
      - id
