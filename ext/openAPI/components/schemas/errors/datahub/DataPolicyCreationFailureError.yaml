allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      reason:
        type: string
        description: The actual reason.
      status:
        type: integer
        default: 400
        description: The HTTP status code.
    required:
      - reason
