allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      id:
        type: string
        description: The script id.
      status:
        type: integer
        default: 409
        description: The Request Conflict HTTP Status Code.
    required:
      - id
