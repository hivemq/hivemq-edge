x-implements:
  - com.hivemq.api.errors.OpenApiError
allOf:
  - $ref: "../../ProblemDetails.yaml"
  - type: object
    properties:
      parameter:
        type: string
        description: The query parameter.
      reason:
        type: string
        description: The reason.
      status:
        type: integer
        default: 400
        description: The HTTP status code.
    required:
      - parameter
      - reason
