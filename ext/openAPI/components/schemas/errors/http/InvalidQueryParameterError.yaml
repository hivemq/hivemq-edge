allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      parameter:
        type: string
        description: The query parameter.
      reason:
        type: string
        description: The reason.
    required:
      - parameter
      - reason
