allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      parameter:
        type: string
        description: The the missing request body parameter.
      status:
        type: integer
        default: 400
        description: The HTTP status code.
