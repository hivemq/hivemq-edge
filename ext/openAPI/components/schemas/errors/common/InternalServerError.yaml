x-implements:
  - com.hivemq.api.errors.OpenApiError
allOf:
  - $ref: "../ApiError.yaml"
  - type: object
    properties:
      reason:
        type: string
        description: The actual reason.
      status:
        type: integer
        default: 500
        description: The HTTP status code.
