get:
  description: Get all managed assets from the Pulse Client
  summary: Get all managed assets
  operationId: get-pulse-managed-assets
  tags:
    - Pulse
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/ManagedAssetList.yaml
      description: Success
    '404':
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/ProblemDetails.yaml
      description: Pulse Client not found
