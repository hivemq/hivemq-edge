type: object
description: List of result items that are returned by this endpoint
properties:
  payload:
    type: string
    description: >-
      The payload of the sample. The bytes are base64 encoded to ensure
      compatibility even if the payload is a arbitrary byte sequence.
required:
  - payload
