type: object
description: >-
  One or more operations the outcome of the validation triggers.  When this
  field is empty, the outcome of the policy validation does not trigger any
  operations.
properties:
  pipeline:
    type: array
    description: >-
      The pipeline to execute, when this action is triggered. The operations in
      the pipeline are executed in-order.
    items:
      $ref: ./PolicyOperation.yaml
