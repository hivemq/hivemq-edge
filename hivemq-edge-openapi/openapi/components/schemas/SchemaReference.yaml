type: object
description: A schema reference is a unique identifier for a schema.
properties:
  schemaId:
    type: string
    description: The identifier of the schema.
  version:
    type: string
    description: >-
      The version of the schema. The value "latest" may be used to always refer
      to the latest schema.
required:
  - schemaId
  - version
