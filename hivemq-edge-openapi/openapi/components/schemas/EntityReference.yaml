type: object
description: >-
  A reference to one of the main entities in Edge (e.g. device, adapter, edge
  broker, bridge host)
properties:
  type:
    $ref: ./EntityType.yaml
  id:
    description: The id of the entity being references in the combiner
    type: string
required:
  - id
  - type
